import { RegisterForm } from "@/features/auth/components/RegisterForm";

export const metadata = {
  title: "Create account | Healthtech",
};

export default function RegisterPage() {
  return <RegisterForm />;
}

